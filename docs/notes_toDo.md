-The rule of five says that if the copy constructor, copy assignment, move constructor, move assignment, or destructor are defined or deleted, then each of those functions should be defined or deleted.


https://en.cppreference.com/cpp/container/list


TODO:
- nu pot plasa o comanda pt ca nu am suficient cash(void Simulator::processAddOrder)
- void Simulator::processAddOrder(const Order& order) - se poate simplifica, codul este aproape acelasi pe ramuri
- elimina OrderLocation struct
